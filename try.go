package main

import (
	"fmt"
	// "math"
)

func main() {
	// fmt.Println(countAndSay(5))
	a := 3
	tm := fmt.Sprintf("%b", a)
	fmt.Println(a, tm, 'b')
}

// func subsets(nums []int) [][]int {
// 	length := len(nums)
// 	sult := int(math.Pow(2, float64(len(nums)))) - 1
// 	// var tmp string
// 	for i := 0; i <= sult; i++ {
// 		tmp := fmt.Sprintf("%b",i)
// 		for j:=0;
// 	}

// }
